version: '3.7'
services:

  metrixpp:
    image: dxworks/metrixpp:1.4
    environment:
      - PROJECT_ID=test
    volumes:
      - /Users/denisfeier/Documents/metrixPP-wrapper:/usr/analysis/sources
      - ./results:/usr/analysis/result
